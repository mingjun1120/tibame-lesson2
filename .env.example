# --- Postgres (consumed by docker-compose) ---
POSTGRES_USER=vms
POSTGRES_PASSWORD=vms_password
POSTGRES_DB=vms
POSTGRES_PORT=5433

# --- pgAdmin (web admin UI) ---
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050
